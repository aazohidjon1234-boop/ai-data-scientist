# Production dependencies for free CPU-only hosting.
#
# Differs from backend/requirements.txt in two ways:
#   * xgboost-cpu instead of xgboost — the default wheel drags in
#     nvidia-nccl-cu13 (~288 MB of CUDA libraries) that a CPU host can never
#     use. The CPU wheel is 5.8 MB and imports under the same `xgboost` name.
#   * no pytest — tests do not run in the deployed image.
fastapi>=0.115
uvicorn[standard]>=0.30
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
scipy>=1.11
joblib>=1.3
xgboost-cpu>=2.1
lightgbm>=4.0
plotly>=5.20
pydantic>=2.7
pydantic-settings>=2.2
SQLAlchemy>=2.0
python-multipart>=0.0.9
httpx>=0.27
psycopg2-binary>=2.9
